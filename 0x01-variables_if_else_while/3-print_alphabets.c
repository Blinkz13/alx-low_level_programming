#include <stdio.h>

/**
 * main - print alphabets in bother upper and lower case
 * Return: 0
 **/

int main(void)
{
	char letter = 'a';

	while (letter <= 'z')
	{
	putchar(letter);
	letter++;
	};

	char letter = 'A';

	while (letter <= 'Z')
	{
	putchar(letterA);
	letterA++;
	}
	putchar('\n');
	return (0);
}
